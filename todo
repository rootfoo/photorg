

an event is a sequence of consequitive photos s.t. 
the gap between photos is less than 5 days


             gap         
 |---------|------|---------------------|----------
1:start   1:end  2:start               2:end

Scan the target directory to find existing events
Scan the source directory and extract metadata from all files
For each source file:
	if metadata contains date
		determine the correct event
			loop over existing event dates until have last_event_before and first_event_after
			get metadata from files in those two directories
			use for 


		copy to event
	else
		copy to unorganized





